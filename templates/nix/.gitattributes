# https://git-scm.com/docs/gitattributes

# end-of-line normalization
* text=auto

## normalize EOL and use native line endings on checkout
*.md text
*.nix text
*.txt text
.editorconfig text
.gitattributes text
.gitignore text
/.reuse/dep5 text
/flake.lock text

# diff
*.md diff=markdown

# linguist
*.lock linguist-generated=true

# TODO: uncomment this section after `nix flake {init,new}`
# # creating an archive
# *.md export-ignore
# *.txt export-ignore
# .editorconfig export-ignore
# .gitattributes export-ignore
# .gitignore export-ignore
# /.reuse/ export-ignore
# /LICENSES/ export-ignore
